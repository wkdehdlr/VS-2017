#include<iostream>
#include<vector>
using namespace std;

vector<int> vec(101, 0);
vector<int> dp(100001);
int N, sum;

int main()
{
	scanf("%d %d", &N, &sum);
	for (int i = 1; i <= N; ++i)
	{
		scanf("%d", &vec[i]);
	}

	for (int i = 1; i <= sum; ++i)
	{
		int tmp;
		for(int )
	}
}